# Intelligent Shopping Assistant (app.py) — Python 3.10+
streamlit>=1.33.0
protobuf==3.20.3
# Backend deps (so frontend can import backend.agent.dialogue in real mode)
openai>=1.40.0
langgraph>=0.2.0
python-dotenv
chromadb
sentence-transformers
datasets
